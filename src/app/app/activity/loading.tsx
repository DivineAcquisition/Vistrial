import { ListPageSkeleton } from "@/components/app/page-skeletons";

export default function ActivityLoading() {
  return (
    <ListPageSkeleton
      title="Activity"
      description="Everything Vistrial is doing, in plain language, as it happens."
    />
  );
}
